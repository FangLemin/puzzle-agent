# Codex Project Memory

- For every PuzzleOps project optimization or implementation update, include the latest local page URL in the final response so the user can open the current UI directly.
- Prefer `http://127.0.0.1:5199/` when the local server is running; include the specific `view` query for the page that was changed.
