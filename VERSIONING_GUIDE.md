# 新手版本管理说明

这个项目现在已经启用 Git 本地版本管理。你可以把 Git 理解成“项目存档系统”：每完成一个阶段，就拍一张快照，以后可以查看、对比、回退。

## 我推荐的版本节奏

- `v0.1.0`：当前 Python 版业务原型基线。
- `v0.2.0`：修复你发现的不如 PRD 的功能点。
- `v0.3.0`：补充简历项目描述、面试 Q&A、核心代码讲解。
- `v1.0.0`：功能和讲述都稳定，可以作为正式展示版。

## 每次修改我会做什么

1. 先确认当前版本已提交，避免覆盖历史。
2. 根据你的修改意见改代码。
3. 跑测试，确认没有把旧功能改坏。
4. 更新 `CHANGELOG.md`，用中文总结这一版完成了什么、还剩什么问题。
5. 提交一个新的 Git commit。
6. 如果是一个阶段性版本，再打 tag，例如 `v0.2.0`。

## 你常用的查看命令

在 VSCode 终端里进入项目：

```bash
cd /Users/fanglemin/Desktop/puzzle-agent-python
```

查看当前有没有未保存进 Git 的修改：

```bash
git status
```

查看历史版本：

```bash
git log --oneline --decorate
```

查看所有版本标签：

```bash
git tag
```

查看阶段总结：

```bash
open CHANGELOG.md
```

## 是否需要上传 GitHub

我建议先本地 Git，等功能稳定后再上传 GitHub。

原因：

- 现在项目还在快速试错，本地版本管理足够安全。
- GitHub 需要决定仓库名、公开还是私有、是否展示给面试官。
- 如果项目里以后加入个人数据或真实业务截图，应该优先用私有仓库。

后续如果你要上传，我推荐：

- 面试前自用：GitHub 私有仓库。
- 简历作品展示：清理敏感信息后，GitHub 公开仓库。
